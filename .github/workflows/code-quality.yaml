name: Code Quality
on:
  push:
    branches: 
      - master
      - main
  pull_request:
jobs:
  lint:
    name: super-lint
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: read
      statuses: write

    steps:
      - uses: actions/checkout@v3
      - name: Lint Code Base
        uses: github/super-linter@v5